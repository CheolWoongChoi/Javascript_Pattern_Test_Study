describe('Conference.attendeeCollection', () => {
  describe('contains(attendee)', () => {
    // contains 테스트
  });

  describe('contains(attendee)', () => {
    // contains 테스트
  });

  describe('contains(attendee)', () => {
    // contains 테스트
  });
});
